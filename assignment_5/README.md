#### Problem Set 5 for Matched Filters

`problem_1.ipynb` is the main file where the majority of the analysis takes place. It makes use of other files such as:
* `read_ligo.ipynb`, to extract the data
